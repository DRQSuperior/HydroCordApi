import { ListenerData } from "../EventEmitter";
import Nest from "../Nest";
export default function useNest<Data>(nest: Nest<Data>, transient?: boolean, filter?: (event: string, data: ListenerData) => boolean): Data;
